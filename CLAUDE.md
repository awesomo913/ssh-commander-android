<!-- claude-backend:generated:start -->
# ssh_commander_android

## Overview

- **Files**: 5 (.py (5))
- **Entry points**: `main.py`

## Structure

```
```

## Conventions

- Type hints are used extensively -- maintain them
- Use `logging.getLogger(__name__)` for all logging
- Absolute imports preferred

## Modules

- `boss_scenarios_v2.py` -- SSH Commander — Boss Mode Scenarios v2
- `commands_expanded.py` -- SSH Commander — expanded command bank
- `engine.py` -- SSH Commander — game engine (no UI dependencies)
- `main.py` -- SSH Commander — Kivy port for Android [entry]
- `progress.py` -- SSH Commander — player progress and achievement system

<!-- claude-backend:generated:end -->
